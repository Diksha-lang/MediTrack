JVM Report

Java uses JVM to run programs. It allows Java to run on any system.

Class Loader loads class files.

Memory areas:
- Heap stores objects
- Stack stores method calls
- Method area stores class data
- PC register tracks execution

Execution engine runs bytecode.

Interpreter runs line by line.
JIT compiler makes execution faster.

JVM makes Java platform independent.